{
  "name": "terraform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sonar": "sonar -Dsonar.host.url=http://localhost:9000 -Dsonar.token=%SONAR_TOKEN% -Dsonar.projectKey=Proyecto -Dsonar.exclusions=deploy/**"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@sonar/scan": "^4.3.2"
  }
}
